#!/usr/bin/env python3
"""Iris CLI - entry point wrapper."""

import sys
from pathlib import Path

# Add brain directory to path so we can import cli
# resolve() follows symlinks to get the actual script location
brain_dir = Path(__file__).resolve().parent / "brain"
sys.path.insert(0, str(brain_dir))

from cli import main

if __name__ == "__main__":
    sys.exit(main() or 0)
